[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = []
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Where to host releases
hosting = "github"
# Build and retain release artifacts when validating pull requests.
pr-run-mode = "upload"
# Run the repository's Rust and project checks before any release build can publish.
plan-jobs = ["./release-checks"]

include = ["THIRD_PARTY_NOTICES.md", "licenses", "docs/dependencies.md"]
